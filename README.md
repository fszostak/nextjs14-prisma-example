# nextjs14-prisma-example

Basic demonstration using Next.js 14 and Prisma, establishing a connection to a MySQL database and executing CRUD operations.

Run the application with Docker.

```
docker-compose up
```
